if ((NOT IS_ARCADE_MODE_ACTIVE()) AND (NOT IS_LMS_TUTORIAL_COMPLETE())) // startup
if ((NOT IS_ARCADE_MODE_ACTIVE()) AND (NOT IS_LMS_TUTORIAL_COMPLETE())) // startup
if ((NOT IS_ARCADE_MODE_ACTIVE()) AND (NOT IS_LMS_TUTORIAL_COMPLETE())) // startup
if ((NOT IS_ARCADE_MODE_ACTIVE()) AND (NOT IS_LMS_TUTORIAL_COMPLETE())) // startup
