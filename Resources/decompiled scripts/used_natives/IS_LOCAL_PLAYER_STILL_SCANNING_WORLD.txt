while (((GET_NETWORK_TIMER() - Result) < 10000) AND (IS_LOCAL_PLAYER_STILL_SCANNING_WORLD())) // startup
while (((GET_NETWORK_TIMER() - Result) < 10000) AND (IS_LOCAL_PLAYER_STILL_SCANNING_WORLD())) // startup
while (((GET_NETWORK_TIMER() - Result) < 10000) AND (IS_LOCAL_PLAYER_STILL_SCANNING_WORLD())) // startup
while (((GET_NETWORK_TIMER() - Result) < 10000) AND (IS_LOCAL_PLAYER_STILL_SCANNING_WORLD())) // startup
while (((GET_NETWORK_TIMER() - Result) < 10000) AND (IS_LOCAL_PLAYER_STILL_SCANNING_WORLD())) // startup
while (((GET_NETWORK_TIMER() - Result) < 10000) AND (IS_LOCAL_PLAYER_STILL_SCANNING_WORLD())) // startup
while (((GET_NETWORK_TIMER() - Result) < 10000) AND (IS_LOCAL_PLAYER_STILL_SCANNING_WORLD())) // startup
while (((GET_NETWORK_TIMER() - Result) < 10000) AND (IS_LOCAL_PLAYER_STILL_SCANNING_WORLD())) // startup
while (((GET_NETWORK_TIMER() - Result) < 10000) AND (IS_LOCAL_PLAYER_STILL_SCANNING_WORLD())) // startup
while (((GET_NETWORK_TIMER() - Result) < 10000) AND (IS_LOCAL_PLAYER_STILL_SCANNING_WORLD())) // startup
while (((GET_NETWORK_TIMER() - Result) < 10000) AND (IS_LOCAL_PLAYER_STILL_SCANNING_WORLD())) // startup
while (((GET_NETWORK_TIMER() - Result) < 10000) AND (IS_LOCAL_PLAYER_STILL_SCANNING_WORLD())) // startup
