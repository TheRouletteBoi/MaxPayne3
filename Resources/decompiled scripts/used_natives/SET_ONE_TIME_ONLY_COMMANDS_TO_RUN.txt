if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
if (SET_ONE_TIME_ONLY_COMMANDS_TO_RUN()) // speechmain
