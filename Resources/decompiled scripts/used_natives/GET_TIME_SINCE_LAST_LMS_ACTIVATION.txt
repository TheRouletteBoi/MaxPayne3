if ((GET_TIME_SINCE_LAST_LMS_ACTIVATION() > 1) AND (GET_TIME_SINCE_LAST_LMS_ACTIVATION() < 15000)) // s_air_becker
if ((GET_TIME_SINCE_LAST_LMS_ACTIVATION() > 1) AND (GET_TIME_SINCE_LAST_LMS_ACTIVATION() < 15000)) // s_air_becker
