if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
if (IS_STREAMING_ADDITIONAL_TEXT( I )) // speechmain
